pub struct Palais {
    pub id: usize,
    pub name: String,
    pub gz_name: String,
    pub daxian: String,
    pub xiaoxian: String,
    pub stars_a: Vec<String>,
    pub stars_b: Vec<String>,
    pub oppo: usize,
    pub tri: (usize, usize),
}
